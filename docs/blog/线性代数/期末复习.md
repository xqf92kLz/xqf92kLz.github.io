???+ warning
	- 区分正交和正定。
	- 不是所有实对称矩阵都能写成 $\boldsymbol B\boldsymbol B^T$ 的形式的。只有半正定的实对称矩阵可以。
	- 证正定首先要证明是实对称矩阵。

???+ tip

	- $(k\boldsymbol A)^*=k^{n-1}\boldsymbol A^*$
	- Jordan 标准型
	- 维数定理
	- Cauchy-Schwarz 不等式：
			
		$|(\boldsymbol \alpha,\boldsymbol \beta)|\leq\|\boldsymbol \alpha\|\|\boldsymbol \beta\|$
			
		$(\boldsymbol \alpha^T\boldsymbol \beta)^2\leq (\boldsymbol \alpha^T\boldsymbol \alpha)(\boldsymbol \beta^T\boldsymbol \beta)$
	- 正定矩阵一定是实对称矩阵
	- 实对称矩阵的属于不同特征值的特征向量两两正交，可以作为条件列方程

## 知识补充

- 秩一矩阵 $\boldsymbol A=\boldsymbol \alpha\boldsymbol \beta^T$
	
	由于 $\boldsymbol A\boldsymbol \alpha=(\boldsymbol \beta^T\boldsymbol \alpha)\boldsymbol \alpha$，故 $\lambda=\boldsymbol \beta^T\boldsymbol \alpha=\text{tr}(\boldsymbol A)$ 是一个特征值。
	
	由于 $r(\boldsymbol A)=1<n$，则 $\lambda=0$ 是至少 $n-1$ 重特征值。
	
	故若 $\text{tr}(\boldsymbol A)\neq 0$，则 $\boldsymbol A$ 的特征值为 $\text{tr}(\boldsymbol A)$（一重）、$0$（$n-1$ 重）。

- 实反对称矩阵

	- 零二次型性质：$\forall \boldsymbol x\in\mathbb P^{n\times 1}$，$y=\boldsymbol x^T\boldsymbol A\boldsymbol x=0$
	
		证明：$y=(\boldsymbol x^T\boldsymbol A\boldsymbol x)^T=\boldsymbol x^T\boldsymbol A^T\boldsymbol x=\boldsymbol x^T(-\boldsymbol A)\boldsymbol x=-y\Rightarrow y=0$
	
	- $|\boldsymbol A|=|\boldsymbol A^T|=|\boldsymbol -\boldsymbol A|=(-1)^n|\boldsymbol A|$
	- 特征值 $\lambda$ 只能是纯虚数或 $0$。非零特征值成对出现（$\pm b\text i$）
	
	- 秩必定是偶数
	
	- $\boldsymbol Q=(\boldsymbol E-\boldsymbol A)(\boldsymbol E+\boldsymbol A)^{-1}$ 是正交矩阵，且不含特征值 $-1$
	
- 方程组 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 与 $\boldsymbol B\boldsymbol x=\boldsymbol 0$ 的公共解：$\begin{pmatrix}\boldsymbol A\\\boldsymbol B\end{pmatrix}\boldsymbol x=\boldsymbol 0$ 的解
- 方程组 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 与 $\boldsymbol B\boldsymbol x=\boldsymbol 0$ 同解 $\Leftrightarrow$ $r(\boldsymbol A)=r(\boldsymbol B)=r\begin{pmatrix}\boldsymbol A\\\boldsymbol B\end{pmatrix}$ $\Leftrightarrow$ $\boldsymbol A$ 的行向量组与 $\boldsymbol B$ 的行向量组等价

	方程组 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解也是 $\boldsymbol B\boldsymbol x=\boldsymbol 0$ 的解 $\Leftrightarrow$ $r(\boldsymbol A)=r\begin{pmatrix}\boldsymbol A\\\boldsymbol B\end{pmatrix}$ $\Leftrightarrow$ $\boldsymbol B$ 的行向量组可以由 $\boldsymbol A$ 的行向量组线性表示


- 舒尔公式：当 $\boldsymbol A$ 可逆时

	$\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\-\boldsymbol C\boldsymbol A^{-1}&\boldsymbol E_{n-r}\end{pmatrix}\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol C&\boldsymbol D\end{pmatrix}=\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol O&\boldsymbol D-\boldsymbol C\boldsymbol A^{-1}\boldsymbol B\end{pmatrix}$
	
	$\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol C&\boldsymbol D\end{pmatrix}\begin{pmatrix}\boldsymbol E_r&-\boldsymbol A^{-1}\boldsymbol B\\\boldsymbol O&\boldsymbol E_{n-r}\end{pmatrix}=\begin{pmatrix}\boldsymbol A&\boldsymbol O\\\boldsymbol C&\boldsymbol D-\boldsymbol C\boldsymbol A^{-1}\boldsymbol B\end{pmatrix}$
	
	$\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\-\boldsymbol C\boldsymbol A^{-1}&\boldsymbol E_{n-r}\end{pmatrix}\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol C&\boldsymbol D\end{pmatrix}\begin{pmatrix}\boldsymbol E_r&-\boldsymbol A^{-1}\boldsymbol B\\\boldsymbol O&\boldsymbol E_{n-r}\end{pmatrix}=\begin{pmatrix}\boldsymbol A&\boldsymbol O\\\boldsymbol O&\boldsymbol D-\boldsymbol C\boldsymbol A^{-1}\boldsymbol B\end{pmatrix}$

- 特征值的降价公式：设 $\boldsymbol A_{m\times n},\boldsymbol B_{n\times m},m\geq n$，则 $|\lambda \boldsymbol E_m-\boldsymbol A\boldsymbol B|=\lambda^{m-n}|\lambda\boldsymbol E_n-\boldsymbol B\boldsymbol A|$。

	特别地，若 $n=m$，则 $\boldsymbol A\boldsymbol B$ 与 $\boldsymbol B\boldsymbol A$ 有相同的特征多项式。

- 列满秩矩阵适合左消去律，行满秩矩阵适合右消去律
- 设 $f(\boldsymbol x)=\boldsymbol x^T\boldsymbol A\boldsymbol x$ 是实二次型 $P_1,\cdots,P_{n-1}$ 非零，则经非退化线性变换后 $f$ 可化为标准形 $f=P_1y_1^2+\dfrac{P_2}{P_1}y_2^2+\cdots+\dfrac{P_n}{P_{n-1}}y_n^2$，其中 $P_n=|\boldsymbol A|$。

## 补习题课内容

### 12.15

- 已知欧式空间 $V_{\mathbb R}$ 中有 $n+1$ 个向量 $\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_n$ 两两之间距离均为 $2$。

	（1）设 $\boldsymbol \beta_i=\boldsymbol \alpha_i-\boldsymbol \alpha_0\,(i=1,2,\cdots,n)$，证明：$i\neq j$ 时，$(\boldsymbol \beta_i,\boldsymbol \beta_j)=2$。
	
	（2）证明：$\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_n$ 线性无关。
	
	??? note "解答"
		- （1）
			
			- **法 1：**
	
  	            $(\boldsymbol \alpha_i-\boldsymbol \alpha_0,\boldsymbol \alpha_j-\boldsymbol \alpha_0)=(\boldsymbol \alpha_i,\boldsymbol \alpha_j)-(\boldsymbol \alpha_i,\boldsymbol \alpha_0)-(\boldsymbol \alpha_j,\boldsymbol \alpha_0)+(\boldsymbol \alpha_0,\boldsymbol \alpha_0)$
	
  	            又 $|\boldsymbol \alpha_i-\boldsymbol \alpha_j|=\sqrt{(\boldsymbol \alpha_i-\boldsymbol \alpha_j,\boldsymbol \alpha_i,\boldsymbol \alpha_j)}=2$，则
	
  	            $4=(\boldsymbol \alpha_i-\boldsymbol \alpha_j,\boldsymbol \alpha_i-\boldsymbol \alpha_j)=(\boldsymbol \alpha_i,\boldsymbol \alpha_i)-2(\boldsymbol \alpha_i,\boldsymbol \alpha_j)+(\boldsymbol \alpha_j,\boldsymbol \alpha_j)\quad(1)$ 
	
  	            $4=(\boldsymbol \alpha_i-\boldsymbol \alpha_0,\boldsymbol \alpha_i-\boldsymbol \alpha_0)=(\boldsymbol \alpha_i,\boldsymbol \alpha_0)-2(\boldsymbol \alpha_i,\boldsymbol \alpha_0)+(\boldsymbol \alpha_0,\boldsymbol \alpha_0)\quad(2)$
	
  	            $4=(\boldsymbol \alpha_j-\boldsymbol \alpha_0,\boldsymbol \alpha_j-\boldsymbol \alpha_0)=(\boldsymbol \alpha_j,\boldsymbol \alpha_j)-2(\boldsymbol \alpha_j,\boldsymbol \alpha_0)+(\boldsymbol \alpha_0,\boldsymbol \alpha_0)\quad(3)$
	
  	            $(2)+(3)-(1)$ 得 $4=2((\boldsymbol \alpha_i,\boldsymbol \alpha_j)-(\boldsymbol \alpha_i,\boldsymbol 0)-(\boldsymbol \alpha_j,\boldsymbol \alpha_0)+(\boldsymbol \alpha_0,\boldsymbol \alpha_0))$
	
  	            因此 $(\boldsymbol \beta_i,\boldsymbol \beta_j)=(\boldsymbol \alpha_i-\boldsymbol \alpha_0,\boldsymbol \alpha_j-\boldsymbol \alpha_0)=2$
		
			- **法 2：**
			
				$(\boldsymbol \beta_i-\boldsymbol \beta_j,\boldsymbol \beta_i-\boldsymbol \beta_j)=(\boldsymbol \beta_i,\boldsymbol \beta_i)-2(\boldsymbol \beta_i,\boldsymbol \beta_j)+(\boldsymbol \beta_j,\boldsymbol \beta_j)$
				
				即 $(\boldsymbol \alpha_i-\boldsymbol \alpha_j,\boldsymbol \alpha_i-\boldsymbol \alpha_j)=(\boldsymbol \alpha_i-\boldsymbol \alpha_0,\boldsymbol \alpha_i-\boldsymbol \alpha_0)-2(\boldsymbol \beta_i,\boldsymbol \beta_j)+(\boldsymbol \alpha_j-\boldsymbol \alpha_0,\boldsymbol \alpha_j-\boldsymbol \alpha_0)$
				
				$4=4-2(\boldsymbol \beta_i,\boldsymbol \beta_j)+4$
				
				得证
		
		- （2）
		
			设 $k_1\boldsymbol \beta_1+\boldsymbol \beta_2+\cdots+k_n\boldsymbol \beta_n=0$
			
			$0=(k_1\boldsymbol \beta_1+\boldsymbol \beta_2+\cdots+k_n\boldsymbol \beta_n,\boldsymbol \beta_i)=k_1(\boldsymbol \beta_1,\boldsymbol \beta_i)+k_2(\boldsymbol \beta_2,\boldsymbol \beta_i)+\cdots+k_n(\boldsymbol \beta_n,\boldsymbol \beta_i)$，$i=1,2,\cdots,n$
			
			$4k_i+2\sum_{j\neq i}k_j=0$
			
			那么对每个 $i$ 都有一个关于 $k_1,k_2,\cdots,k_n$ 的方程，只需证 $n$ 个未知数、$n$ 个方程的方程组只有零解
			
			即 $\boldsymbol A=\begin{pmatrix}4&2&\cdots&2\\2&4&\cdots&2\\\vdots&\vdots&\ddots&\vdots\\2&2&\cdots&4\end{pmatrix}$，$\boldsymbol x=\begin{pmatrix}k_1\\k_2\\\vdots\\k_n\end{pmatrix}$，证明 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 只有零解
			
			$|\boldsymbol A|=2^{n-1}\cdot (2n+2)=2^n(n+1)\neq 0$，故 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 只有零解，故 $k_1=k_2=\cdots=k_n=0$

### 12.22

- P220.6

	设 $\boldsymbol \alpha,\boldsymbol \beta$ 是 $n$ 维欧式空间 $V$ 中的两个不同的向量且 $|\boldsymbol \alpha|=|\boldsymbol \beta|=1$，证明：$(\boldsymbol \alpha,\boldsymbol \beta)\neq 1$。

	??? note "解答"
		- **法 1：**
		
            $\boldsymbol \alpha-\boldsymbol \beta\neq 0$
        
            则 $(\boldsymbol \alpha-\boldsymbol \beta,\boldsymbol \alpha-\boldsymbol \beta)=(\boldsymbol \alpha,\boldsymbol \alpha)-2(\boldsymbol \alpha,\boldsymbol \beta)+(\boldsymbol \beta,\boldsymbol \beta)>0$
        
            故 $(\boldsymbol \alpha,\boldsymbol \beta)<1$
        
            ???+ tip
                出现 $(\boldsymbol \alpha,\boldsymbol beta)$，考虑 $(\boldsymbol \alpha+\boldsymbol \beta,\boldsymbol \alpha+\boldsymbol \beta)$，$(\boldsymbol \alpha-\boldsymbol \beta,\boldsymbol \alpha-\boldsymbol \beta)$
        
	    - **法 2：**
	    
	    	将 $\boldsymbol \alpha$ 扩充为 $V$ 的标准正交基 $\boldsymbol \alpha,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_n$
	    	
	    	$\boldsymbol \beta\in V$，则设 $\boldsymbol \beta=k_1\boldsymbol \alpha+k_2\boldsymbol \alpha_2+\cdots+k_n\boldsymbol \alpha_n$
	    	
	    	假设 $(\boldsymbol \alpha,\boldsymbol \beta)=1$，则 $(\boldsymbol \alpha,k_1\boldsymbol \alpha+k_2\boldsymbol \alpha_2+\cdots+k_n\boldsymbol \alpha_n)=k_1(\boldsymbol \alpha,\boldsymbol \alpha)+k_2(\boldsymbol \alpha,\boldsymbol \alpha_2)+\cdots+k_n(\boldsymbol \alpha,\boldsymbol \alpha_n)=k_1$
	    	
	    	故 $\boldsymbol \beta=\boldsymbol \alpha+k_2\boldsymbol \alpha_2+\cdots+k_n\boldsymbol \alpha_n$
	    	
	    	又 $|\boldsymbol \beta|=1\Rightarrow (\boldsymbol \beta,\boldsymbol \beta)=1$，则 $(\boldsymbol \alpha+k_2\boldsymbol \alpha_2+\cdots+k_n\boldsymbol \alpha_n,\boldsymbol \alpha+k_2\boldsymbol \alpha_2+\cdots+k_n\boldsymbol \alpha_n)=1+k_2^2+\cdots+k_n^2=$，得 $k_2=k_3=\cdots=k_n=0$，从而 $\boldsymbol \beta=\boldsymbol \alpha$，这与 $\boldsymbol \alpha,\boldsymbol \beta$ 不同矛盾
	    	
	    	因此 $(\boldsymbol \alpha,\boldsymbol \beta)\neq 1$
	    	
	    	???+ tip
	    		欧式空间找一个标准正交基，用标准正交基的性质
	
- 设 $\boldsymbol A$ 为 $m\times n$ 的实矩阵，证明：$r(\boldsymbol A^T\boldsymbol A)=r(\boldsymbol A\boldsymbol A^T)=r(\boldsymbol A)$

	??? note "解答"
		
		
		- 证明：$r(\boldsymbol A^T\boldsymbol A)=r(\boldsymbol A)$
		
			只需证 $\boldsymbol A^T\boldsymbol A\boldsymbol x=\boldsymbol 0$ 与 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 同解
			
			若 $\boldsymbol A\boldsymbol \alpha=0$，则 $\boldsymbol A^T\boldsymbol A\boldsymbol \alpha=\boldsymbol 0$。故 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解是 $\boldsymbol A^T\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解
			
			若 $\boldsymbol A^T\boldsymbol A\boldsymbol \alpha=\boldsymbol 0$，则 $\boldsymbol \alpha^T\boldsymbol A^T\boldsymbol A\boldsymbol \alpha=\boldsymbol 0$，即 $(\boldsymbol A\boldsymbol \alpha)^T(\boldsymbol A\boldsymbol \alpha)=\boldsymbol 0$，故 $\boldsymbol A\boldsymbol \alpha=\boldsymbol 0$。因此 $\boldsymbol A^T\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解是 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解
		
		- 证明：$r(\boldsymbol A\boldsymbol A^T)=r(\boldsymbol A)$
		
			已证 $r(\boldsymbol A^T\boldsymbol A)=r(\boldsymbol A)$，则 $r(\boldsymbol A\boldsymbol A^T)=r(\boldsymbol A^T)=r(\boldsymbol A)$
  	  
		???+ tip
			若 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 与 $\boldsymbol B\boldsymbol x=\boldsymbol 0$ 同解，则 $r(\boldsymbol A)=r(\boldsymbol B)$

- P155.6

	设 $\boldsymbol A$ 是一个可逆矩阵，证明：存在多项式 $f(x)$ 使得 $\boldsymbol A^{-1}=f(\boldsymbol A)$。
	
	??? note "解答"
		$f(\lambda)=|\lambda\boldsymbol E-\boldsymbol A|=\lambda^n-\text{tr}(\boldsymbol A)\lambda^{n-1}+\cdots+(-1)^n|\boldsymbol A|$
		
		设特征多项式为 $f(x)=x^n+a_1x^{n-1}+\cdots+a_{n-1}x+a_n$
		
		由于 $\boldsymbol A$ 可逆，则 $a_n\neq 0$
		
		再由 Hamilton-Cayley 定理，$f(\boldsymbol A)=\boldsymbol A^n+a_1\boldsymbol A^{n-1}+\cdots+a_{n-1}\boldsymbol A+a_n\boldsymbol E=\boldsymbol O$
		
		$\boldsymbol A(\boldsymbol A^{n-1}+a_1\boldsymbol A^{n-2}+\cdots+a_{n-1}\boldsymbol E)=-a_n\boldsymbol E$
		
		$\boldsymbol A(-\dfrac 1 {a_n}\boldsymbol A^{n-1}-\dfrac{a_1}{a_n}\boldsymbol A^{n-2}-\cdots-\dfrac{a_{n-1}}{a_n}\boldsymbol E)=\boldsymbol E$
		
		取 $g(x)=-\dfrac 1 {a_n}x^{n-1}-\dfrac{a_1}{a_n}x^{n-2}-\cdots-\dfrac{a_{n-1}}{a_n}$，则 $\boldsymbol A^{-1}=g(\boldsymbol A)$
  
- 设 $\boldsymbol A\in\mathbb P^{3\times 3}$，$|-\boldsymbol E-\boldsymbol A|=-1$，$|\boldsymbol E-\boldsymbol A|=1$，$|2\boldsymbol E-\boldsymbol A|=2$，求 $|3\boldsymbol E-\boldsymbol A|$。

	??? note "解答"
		设 $f(\lambda)=|\lambda\boldsymbol E-\boldsymbol A|=\lambda^3+a\lambda^2+b\lambda+c$
		
		则 $f(-1)=-1$，$f(1)=1$，$f(2)=2$，解得 $a=-2,b=0,c=2$
		
		$f(\lambad)=\lambda^3-2\lambda^2+2$，则 $f(3)=11$

### 12.29

- P84.6.(2)

	设 $\boldsymbol A\in\mathbb P^{m\times n},\boldsymbol B\in\mathbb P^{n\times m}$，则 $\lambda\neq 0$ 时，$\lambda^n|\lambda\boldsymbol E_m-\boldsymbol A\boldsymbol B|=\lambda^m|\lambda\boldsymbol E_n-\boldsymbol B\boldsymbol A|$

- P149.3

	若 $r(\boldsymbol A)=1$，则 $\boldsymbol A$ 的特征值为 $\text{tr}(\boldsymbol A)$，$0$（$n-1$ 重）。
	
	??? note "解答"
		设 $\boldsymbol A=\boldsymbol \alpha\boldsymbol \beta^T$
		
		- **法 1：**
		
			$|\lambda\boldsymbol E_n-\boldsymbol \alpha\boldsymbol \beta^T|=\lambda^{n-1}|\lambda\boldsymbol E_1-\boldsymbol \beta^T\boldsymbol \alpha|=\lambda^{n-1}(\lambda-\boldsymbol \beta^T\boldsymbol \alpha)=0$
		
		- **法 2：**
		
			$\begin{vmatrix}\lambda\boldsymbol E&\boldsymbol \alpha\\\boldsymbol \beta^T&1\end{vmatrix}=|\lambda\boldsymbol E_n||1-\boldsymbol \beta^T\dfrac 1 {\lambda}\boldsymbol \alpha|=1\cdot|\lambda\boldsymbol E_n-\boldsymbol \alpha\boldsymbol \beta^T|$，则 $|\lambda\boldsymbol E_n-\boldsymbol \alpha\boldsymbol \beta^T|=\lambda^n(1-\boldsymbol \beta^T\dfrac 1 {\lambda}\boldsymbol \alpha)=\lambda^{n-1}(\lambda-\boldsymbol \beta^T\boldsymbol \alpha)$
		
		$\text{tr}(\boldsymbol A)=\text{tr}(\boldsymbol \alpha\boldsymbol \beta^T)=\text{tr}(\boldsymbol \beta^T\boldsymbol \alpha)=\boldsymbol \beta^T\boldsymbol \alpha$
		
		则 $\lambda_1=\lambda_2=\cdots=\lambda_{n-1}=0$，$\lambda_n=\boldsymbol \beta^T\boldsymbol \alpha-\text{tr}(\boldsymbol A)$

- 2024-2025 春夏期末

	设 $n$ 阶矩阵 $\boldsymbol A$ 的每一行或每一列的元素之和均为 $0$，证明：$\boldsymbol A$ 的每个元素的代数余子式都相等。
	
	??? note "解答"
		$\boldsymbol A(1,1,\cdots,1)^T=\boldsymbol 0$，则 $|\boldsymbol A|=0$，设 $\boldsymbol \alpha=(1,1,\cdots,1)^T$
		
		$\boldsymbol A\boldsymbol A^*=|\boldsymbol A|\boldsymbol E=\boldsymbol O$
		
		$r(\boldsymbol A^*)=\begin{cases}n,&r(\boldsymbol A)=n\\1,&r(\boldsymbol A)=n-1\\0,&r(\boldsymbol A)<n-1\end{cases}$ 考虑分类
		
		由于 $r(\boldsymbol A)\leq n-1$
		
		1. 若 $r(\boldsymbol A)<n-1$，则 $r(\boldsymbol A^*)=0$，$\boldsymbol A^*=\boldsymbol O$
		2. 若 $r(\boldsymbol A)=n-1$，则 $\dim W=1$，$\boldsymbol \alpha$ 可作为解空间 $W$ 的基向量
		
			又 $\boldsymbol A\boldsymbol A*=\boldsymbol O$ 说明 $\boldsymbol A^*=(\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_n)$ 的每一列都是 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的解，所以 $\boldsymbol \beta_i=k_i\boldsymbol \alpha$，则 $\boldsymbol A^*$ 每一列元素都一样
			
			同理 $\boldsymbol A^T(1,1,\cdots,1)^T=\boldsymbol 0$，$(\boldsymbol A^T)^*$ 即 $(\boldsymbol A^*)^T$ 的每一列元素都相等
	

## 刷历年卷

???+ info "2024-2025 春夏"
	已知 $n$ 阶方阵 $\boldsymbol A$ 满足 $\boldsymbol \alpha\boldsymbol A^{n-1}\neq \boldsymbol 0^T$，$\alpha \boldsymbol A^n=\boldsymbol 0^T$，则 $\boldsymbol A$ 的属于特征值 $0$ 的特征子空间维数是 ______。
	
	??? note "解答"
		取 $\boldsymbol A=J_n(0)$，则 $n-r(\boldsymbol A)=n-(n-1)=1$。
		
		答案：$1$。
	
	已知 $\mathbb P=\{f(x)\mid \deg f(x)\leq 3\}$。当 $f(x)$ 满足 $f(1)=0,f(-1)=$______ 时，$\mathbb P$ 的满足该条件的子集是一个子空间，该子空间 $V$ 的维数是 ______，它的一组基是 ______________。定义该空间的内积 $(f(x),g(x))=\int_{-1}^1 f(x)g(x)\text dx$，则它的一组标准正交基是 _______________。
	
	??? note "解答"
		- 第一空：零多项式 $f(x)=0\cdot x^3+0\cdot x^2+0\cdot x+0$ 必定在子空间中，若满足 $f(1)=0$，也必满足 $f(-1)=0$。
		- 第二空、第三空：
			
			- **法 1：**
			
				设 $f(x)=ax^3+bx^2+cx+d$，则 $f(1)=a+b+c+d=0$，$f(-1)=-a+b-c+d=0$，得 $a=c$ 且 $b=d$。故维数为 $4-2=2$，基可取 $\{x^3-x,x^2-1\}$。
			
			- **法 2：**
			
				若 $f(1)=0,f(-1)=0$，则 $f(x)$ 有因式 $(x-1)(x+1)=x^2-1$，则 $f(x)=(x^2-1)(ax+b)=a(x^2-1)+b(x^3-x)$。$\{x^2-1,x^3-x\}$ 线性无关，且能表示子空间中的所有多项式。故 $V$ 的维数是 $2$，一组基是 $\{x^2-1,x^3-x\}$。
		- 第四空：
			
			设 $\boldsymbol \alpha_1=x^2-1$，$\boldsymbol \alpha_2=x^3-x$。
			
			- 正交化：
			
				$\boldsymbol \beta_1=\boldsymbol \alpha_1=x^2-1$
				
				$\boldsymbol \beta_2=\boldsymbol \alpha_2-\dfrac{(\boldsymbol \alpha_2,\boldsymbol \beta_1)}{(\boldsymbol \beta_1,\boldsymbol \beta_1)}\boldsymbol \beta_1=\boldsymbol \alpha_2-\dfrac{0}{(\boldsymbol \beta_1,\boldsymbol \beta_1)}\boldsymbol \beta_1=\boldsymbol \alpha_2=x^3-x$
			
			- 单位化：
			
				$(\boldsymbol \beta_1,\boldsymbol \beta_1)=\int_{-1}^1(x^2-1)^2\text dx=2\int_0^1 (x^4-2x^2+1)\text dx=2(\dfrac 1 5-\dfrac 2 3 +1)=\dfrac{16}{15}$
				
				$\boldsymbol \gamma_1=\dfrac{\boldsymbol \beta_1}{\|\boldsymbol \beta_1\|}=\dfrac{\boldsymbol \beta_1}{\frac 4 {\sqrt{15}}}=\dfrac {\sqrt{15}}4(x^2-1)$
				
				$\|\boldsymbol \beta_2\|=\int_{-1}^1(x^3-x)^2\text dx=2\int_0^1 (x^6-2x^4+x^2)\text dx=2(\dfrac 1 7-\dfrac 2 5+\dfrac 1 3)=\dfrac{16}{105}$
				
				$\boldsymbol \gamma_2=\dfrac{\boldsymbol \beta_2}{\|\boldsymbol \beta_2\|}=\dfrac{\boldsymbol \beta_2}{\frac 4 {\sqrt{105}}}=\dfrac{\sqrt{105}}4(x^3-x)$
			
			故一组标准正交基为 $\{\dfrac{\sqrt{15}}4(x^2-1),\dfrac{\sqrt{105}}4(x^3-x)\}$。
		
		答案：$0$；$2$；$x^2-1,x^3-x$；$\dfrac{\sqrt{15}}4(x^2-1),\dfrac{\sqrt{105}}4(x^3-x)$。
	
	已知 $\boldsymbol A,\boldsymbol B,\boldsymbol C$ 为 $n$ 阶实方阵，$\boldsymbol S=\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol O&\boldsymbol C\end{pmatrix}$，若 $\boldsymbol S\boldsymbol S^T=\boldsymbol S^T\boldsymbol S$，证明：$\boldsymbol B=\boldsymbol O$。
	
	??? note "证明"
		$\boldsymbol S\boldsymbol S^T=\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol O&\boldsymbol C\end{pmatrix}\begin{pmatrix}\boldsymbol A^T&\boldsymbol O\\\boldsymbol B^T&\boldsymbol C^T\end{pmatrix}=\begin{pmatrix}\boldsymbol A\boldsymbol A^T+\boldsymbol B\boldsymbol B^T&\boldsymbol B\boldsymbol C^T\\\boldsymbol C\boldsymbol B^T&\boldsymbol C\boldsymbol C^T\end{pmatrix}$
		
		$\boldsymbol S^T\boldsymbol S=\begin{pmatrix}\boldsymbol A^T&\boldsymbol O\\\boldsymbol B^T&\boldsymbol C^T\end{pmatrix}\begin{pmatrix}\boldsymbol A&\boldsymbol B\\\boldsymbol O&\boldsymbol C\end{pmatrix}=\begin{pmatrix}\boldsymbol A^T\boldsymbol A&\boldsymbol A^T\boldsymbol B\\\boldsymbol B^T\boldsymbol A&\boldsymbol B^T\boldsymbol B+\boldsymbol C^T\boldsymbol C\end{pmatrix}$
		
		则 $\boldsymbol A\boldsymbol A^T+\boldsymbol B\boldsymbol B^T=\boldsymbol A^T\boldsymbol A$
		
		$\text{tr}(\boldsymbol A\boldsymbol A^T)+\text{tr}(\boldsymbol B\boldsymbol B^T)=\text{tr}(\boldsymbol A^T\boldsymbol A)$
		
		又 $\text{tr}(\boldsymbol A\boldsymbol A^T)=\text{tr}(\boldsymbol A^T\boldsymbol A)$，则 $\text{tr}(\boldsymbol B\boldsymbol B^T)=\sum_{i=1}^n\sum_{j=1}^n b_{ij}^2=0$，则 $\boldsymbol B=\boldsymbol O$
		
		???+ tip
			$\text{tr}(\boldsymbol A\boldsymbol A^T)\geq 0$，且 $\text{tr}(\boldsymbol A\boldsymbol A^T)=0\Leftrightarrow \boldsymbol A=\boldsymbol O$
	
	设 $n$ 阶方阵 $\boldsymbol A$ 满足 $(1,2,\cdots,n)\boldsymbol A=\boldsymbol 0^T$ 且 $\boldsymbol A(1,2,\cdots,n)^T=\boldsymbol 0$，其中 $\boldsymbol 0$ 为 $n$ 维零列向量。证明：存在 $n$ 维列向量 $\boldsymbol \alpha$ 使得 $\boldsymbol A^*=\boldsymbol \alpha\boldsymbol \alpha^T$。
	
	??? note "解答"
		令 $\boldsymbol \beta=(1,2,\cdots,n)^T$，则 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 有非零解 $\boldsymbol \beta$，故 $r(\boldsymbol A)<n$。
		
		- 若 $r(\boldsymbol A)<n-1$，则 $\boldsymbol A^*=\boldsymbol O$，取 $\boldsymbol \alpha=\boldsymbol 0$ 即可。
		- 若 $r(\boldsymbol A)=n-1$：
			
			$n-r(\boldsymbol A)=1$，$\boldsymbol \beta$ 是 $\boldsymbol A\boldsymbol x=\boldsymbol O$ 的一个基础解系。
			
			又 $\boldsymbol A\boldsymbol A^*=|\boldsymbol A|\boldsymbol E=\boldsymbol O$，则 $\boldsymbol A^*$ 的每个列向量均为 $\boldsymbol A\boldsymbol x=\boldsymbol O$ 的解，可得 $\boldsymbol A^*$ 的列向量都是 $\boldsymbol \beta$ 的倍数。
			
			同理可得 $\boldsymbol A^*$ 的所有行向量都是 $\boldsymbol \beta^T$ 的倍数。
			
			那么 $\boldsymbol A^*=k\cdot \boldsymbol \beta\boldsymbol \beta^T$，取 $\boldsymbol \alpha=\sqrt k\cdot \boldsymbol \beta$，则 $\boldsymbol A^*=\boldsymbol \alpha\boldsymbol \alpha^T$。

???+ info "2024-2025 秋冬"
	设 $\boldsymbol \alpha=(1,1,\cdots,1)^T\in\mathbb P^{n\times 1}$，$\boldsymbol J=\boldsymbol \alpha\boldsymbol \alpha^T$。（1）$\boldsymbol J^2=n\boldsymbol J$ 是否成立？（2）求 $(\boldsymbol J-\boldsymbol E)^{-1}$。
	
	??? note "证明"
		（1）$\boldsymbol J^2=(\boldsymbol \alpha\boldsymbol \alpha^T)(\boldsymbol \alpha\boldsymbol \alpha^T)=\boldsymbol \alpha(\boldsymbol \alpha^T\boldsymbol \alpha)\boldsymbol \alpha^T=n\boldsymbol \alpha\boldsymbol \alpha^=n\boldsymbol J$
		
		（2）$\boldsymbol J^2-n\boldsymbol J=\boldsymbol O$，$(\boldsymbol J-\boldsymbol E)(\boldsymbol J-(n-1)\boldsymbol E)=(n-1)\boldsymbol E$，$\Rightarrow (\boldsymbol J-\boldsymbol E)^{-1}=\dfrac{1}{n-1}\boldsymbol J-\boldsymbol E$
	
	设 $\boldsymbol A^T=-\boldsymbol A$，证明：（1）$\boldsymbol E-\boldsymbol A$ 可逆；（2）$\boldsymbol Q=(\boldsymbol E-\boldsymbol A)^{-1}(\boldsymbol E+\boldsymbol A)$ 为正交矩阵。
	
	??? note "解答"
		- （1）
		
			- **法 1：**
			
	            考虑齐次线性方程组 $(\boldsymbol E-\boldsymbol A)\boldsymbol x=\boldsymbol 0$。
	
	            则 $\boldsymbol x^T(\boldsymbol E-\boldsymbol A)\boldsymbol x=\boldsymbol x^T\boldsymbol 0=0$
	
	            $\boldsymbol x^T\boldsymbol x-\boldsymbol x^T\boldsymbol A\boldsymbol x=0$
	
	            而 $\boldsymbol x^T\boldsymbol A\boldsymbol x=(\boldsymbol x^T\boldsymbol A\boldsymbol x)^T=\boldsymbol x^T(-\boldsymbol A)\boldsymbol x=-\boldsymbol x^T\boldsymbol A\boldsymbol x$，得 $\boldsymbol x^T\boldsymbol A\boldsymbol x=0$
	
	            故 $\boldsymbol x^T\boldsymbol x=0\Rightarrow \boldsymbol x=0$，从而齐次方程组 $(\boldsymbol E-\boldsymbol A)\boldsymbol x=\boldsymbol 0$ 只有零解，故 $\boldsymbol E-\boldsymbol A$ 可逆。
	
	            ???+ tip
	                证明 $\boldsymbol A$ 可逆，除了证明 $|\boldsymbol A|\neq 0$ 外，还可以证明齐次线性方程组 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 只有零解。
	
			- **法 2：**
			
				实反对称矩阵 $\boldsymbol A$ 的特征值只能是 $0$ 或纯虚数 $b\text i$。$\boldsymbol E+\boldsymbol A$ 的特征值为 $1$ 或 $1+b\text i$，故 $\boldsymbol E+\boldsymbol A$ 的所有特征值均不为 $0$，$\boldsymbol E+\boldsymbol A$ 可逆。
		
		- （2）
		
			$\boldsymbol Q^T=[(\boldsymbol E-\boldsymbol A)^{-1}(\boldsymbol E+\boldsymbol A)]^T=(\boldsymbol E+\boldsymbol A)^T[(\boldsymbol E-\boldsymbol A)^{-1}]^T=(\boldsymbol E-\boldsymbol A)\cdot (\boldsymbol E+\boldsymbol A)^{-1}$
			
			$\boldsymbol Q^T\boldsymbol Q=(\boldsymbol E-\boldsymbol A)(\boldsymbol E+\boldsymbol A)^{-1}(\boldsymbol E-\boldsymbol A)^{-1}(\boldsymbol E+\boldsymbol A)$
			
			由于 $\boldsymbol E-\boldsymbol A$ 与 $\boldsymbol E+\boldsymbol A$ 可交换，则它们的逆也可交换
			
			得 $\boldsymbol Q^T\boldsymbol Q=(\boldsymbol E-\boldsymbol A)(\boldsymbol E-\boldsymbol A)^{-1}(\boldsymbol E+A)^{-1}(\boldsymbol E+\boldsymbol A)=\boldsymbol E$，得证
	
	$\boldsymbol A\in\mathbb P^{n\times n}$，$r(\boldsymbol A)=r$，证明：（1）$\exists \boldsymbol P$，使得 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P$ 的后 $n-r$ 行全为 $0$；（2）若 $r(\boldsymbol E-\boldsymbol A)=n-r$，则 $\boldsymbol A^2=\boldsymbol A$。
	
	??? note "解答"
		- （1）
		
			设 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol Q^{-1}=\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\\boldsymbol O&\boldsymbol O\end{pmatrix}$
			
			则 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol P^{-1}\boldsymbol A\boldsymbol Q^{-1}\boldsymbol Q\boldsymbol P=\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\\boldsymbol O&\boldsymbol O\end{pmatrix}\begin{pmatrix}\boldsymbol C&\boldsymbol D\\\boldsymbol F&\boldsymbol G\end{pmatrix}=\begin{pmatrix}\boldsymbol C&\boldsymbol D\\\boldsymbol O&\boldsymbol O\end{pmatrix}$
			
		- （2）
		
			$\dim V_0=n-r(0\cdot \boldsymbol E-\boldsymbol A)=n-r$
	
	        $\dim V_1=n-r(1\cdot \boldsymbol E-\boldsymbol A)=r$
	        
	        由于 $(n-r)+r=n$，故 $\boldsymbol A$ 的特征值为 $0$（$n-r$ 重）或 $1$（$r$ 重）。
	
	        则 $\exists \boldsymbol P$，$\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda=\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\\boldsymbol O&\boldsymbol O\end{pmatrix}$，则 $\boldsymbol A^2=\boldsymbol P\boldsymbol \Lambda^2\boldsymbol P^{-1}=\boldsymbol A$。

???+ info "2023-2024"
	设 $\mathbb R^{3\times 3}$ 是所有三阶实方阵的全体在矩阵的加法和实数与矩阵的数乘运算下所构成的实线性空间。已知 $\boldsymbol A=\begin{pmatrix}0&0&0\\1&0&0\\0&1&0\end{pmatrix}$，令 $\boldsymbol W=\{\boldsymbol X\in\mathbb R^{3\times 3}\mid  \boldsymbol A\boldsymbol X=\boldsymbol X\boldsymbol A\}$。
	
	（1）证明：$\boldsymbol W$ 是 $\mathbb R^{3\times 3}$ 的子空间；
	
	（2）假设有矩阵 $\boldsymbol B=(b_{ij})_{3\times 3}$ 使得 $\boldsymbol B\boldsymbol A=\boldsymbol A\boldsymbol B$，证明：$\boldsymbol B=b_{11}\boldsymbol E+b_{21}\boldsymbol A+b_{31}\boldsymbol A^2$；
	
	（3）求 $\dim W$。
	
	??? note "解答"
	
		- （1）
			
			集合非空：$\boldsymbol O\boldsymbol A=\boldsymbol A\boldsymbol O$，则 $\boldsymbol O\in\boldsymbol W$
			
			加法封闭：设 $\boldsymbol X_1,\boldsymbol X_2\in W$，则 $\boldsymbol A(\boldsymbol X_1+\boldsymbol X_2)=\boldsymbol A\boldsymbol X_1+\boldsymbol A\boldsymbol X_2=\boldsymbol X_1\boldsymbol A+\boldsymbol X_2\boldsymbol A=(\boldsymbol X_1+\boldsymbol X_2)\boldsymbol A$，故 $\boldsymbol X_1+\boldsymbol X_2\in W$
			
			数乘封闭：设 $k\in\mathbb R$，$\boldsymbol X\in W$，则 $\boldsymbol A(k\boldsymbol X)=k(\boldsymbol A\boldsymbol X)=k(\boldsymbol X\boldsymbol A)=(k\boldsymbol X)\boldsymbol A$，故 $k\boldsymbol X\in W$
			
			综上，$W$ 是 $\mathbb R^{3\times 3}$ 的子空间。
		
		- （2）
			
			$\boldsymbol A^2=\begin{pmatrix}0&0&0\\0&0&0\\1&0&0\end{pmatrix}$
			
			$\boldsymbol A\boldsymbol B=\begin{pmatrix}0&0&0\\b_{11}&b_{12}&b_{13}\\b_{21}&b_{22}&b_{23}\end{pmatrix}$
			
			$\boldsymbol B\boldsymbol A=\begin{pmatrix}b_{12}&b_{13}&0\\b_{22}&b_{23}&0\\b_{32}&b_{33}&0\end{pmatrix}$
			
			则 $b_{12}=b_{13}=b_{23}=0$，$b_{11}=b_{22}=b_{33}$，$b_{21}=b_{32}$
			
			则 $\boldsymbol B=\begin{pmatrix}b_{11}&0&0\\b_{21}&b_{11}&0\\b_{31}&b_{21}&b_{11}\end{pmatrix}=b_{11}\boldsymbol E+b_{21}\boldsymbol A+b_{31}\boldsymbol A^2$
		
		- （3）
		
			由（2）知，任意 $\boldsymbol X\in W$ 均可表示为 $k_1\boldsymbol E+k_2\boldsymbol A+k_3\boldsymbol A^2$。
			
			又 $\{\boldsymbol E,\boldsymbol A,\boldsymbol A^2\}$ 线性无关，故构成一组基，$\dim W=3$。
	
	设 $\boldsymbol A_{ij}$ 是 $n\,(n\geq 2)$ 阶实对称矩阵，$\boldsymbol B$ 是所有元素均为 $1$ 的 $n$ 阶方阵，已知 $r(\boldsymbol A)=n-1$ 且 $\boldsymbol A$ 每行的元素之和均为 $0$。证明：$\exists k\in\mathbb R$ 使得 $\boldsymbol A^*=k\boldsymbol B$。
	
	??? note "解答"
		设 $\xi=(1,1,\cdots,1)^T$。
		
		$\boldsymbol A$ 每行的元素之和均为 $0$ $\Rightarrow \boldsymbol A\xi=\boldsymbol 0$
		
		$n-r(\boldsymbol A)=1$，故 $\xi$ 是 $\boldsymbol A\boldsymbol x=\boldsymbol 0$ 的一个基础解系。
		
		又 $\boldsymbol A\boldsymbol A*=|\boldsymbol A|\boldsymbol E=O$，故 $\boldsymbol A^*$ 的每个列向量都是 $\xi$ 的倍数，即 $\boldsymbol A^*$ 每列元素相同。
		
		又 $\boldsymbol A$ 实对称 $\Rightarrow \boldsymbol A^*$ 实对称，则 $\boldsymbol A^*$ 每行元素相同。
		
		故 $\boldsymbol A^*$ 所有元素相同，得证。
	
	设 $\boldsymbol A$ 是一个 $n$ 阶正定矩阵，$\boldsymbol \alpha,\boldsymbol \beta$ 为任意两个 $n$ 维实列向量，证明：$(\boldsymbol \alpha^T\boldsymbol \beta)^2\leq (\boldsymbol \alpha^T\boldsymbol A\boldsymbol \alpha)\cdot(\boldsymbol \beta^T\boldsymbol A^{-1}\boldsymbol \beta)$。
	
	??? note "解答"
		
		$\boldsymbol A$ 正定 $\Rightarrow$ $\exists$ 可逆实矩阵 $\boldsymbol C$ 使得 $\boldsymbol A=\boldsymbol C^T\boldsymbol C$
		
		$\begin{aligned}(\boldsymbol \alpha^T\boldsymbol A\boldsymbol \alpha)\cdot (\boldsymbol \beta^T\boldsymbol A^{-1}\boldsymbol \beta)&=\boldsymbol \alpha^T\boldsymbol C^T\boldsymbol C\boldsymbol \alpha\cdot \boldsymbol \beta^T(\boldsymbol C^{-1})^T\boldsymbol C^{-1}\boldsymbol \beta\\&=(\boldsymbol C\boldsymbol \alpha)^T(\boldsymbol C\boldsymbol \alpha)\cdot (\boldsymbol C^{-1}\boldsymbol \beta)^T(\boldsymbol C^{-1}\boldsymbol \beta)\\&\geq ((\boldsymbol C\boldsymbol \alpha)^T(\boldsymbol C^{-1}\boldsymbol \beta))^2\\&=(\boldsymbol \alpha^T\boldsymbol \beta)^2\end{aligned}$
		
		???+ tip
			Cauchy-Schwarz 不等式：
			
			$|(\boldsymbol \alpha,\boldsymbol \beta)|\leq\|\boldsymbol \alpha\|\|\boldsymbol \beta\|$
			
			$(\boldsymbol \alpha^T\boldsymbol \beta)^2\leq (\boldsymbol \alpha^T\boldsymbol \alpha)(\boldsymbol \beta^T\boldsymbol \beta)$
	
	设 $\boldsymbol A,\boldsymbol B,\boldsymbol C,\boldsymbol D$ 是 $2^{2024}$ 阶实方阵，且 $\forall k\in\mathbb Z^+$，$\begin{vmatrix}k\boldsymbol E+\boldsymbol A&\boldsymbol B\\\boldsymbol C&\boldsymbol D\end{vmatrix}=0$，证明：$|\boldsymbol D|=0$。
	
	??? note "解答"
		假设 $|\boldsymbol D| \neq 0$，则 $\boldsymbol D$ 可逆。
	
	    当 $\boldsymbol D$ 可逆时，$\begin{vmatrix}k\boldsymbol E+\boldsymbol A & \boldsymbol B \\\boldsymbol C & \boldsymbol D\end{vmatrix} = |\boldsymbol D| \cdot | (k\boldsymbol E+\boldsymbol A) - \boldsymbol B \boldsymbol D^{-1} \boldsymbol C |\xlongequal{\boldsymbol P = \boldsymbol A - \boldsymbol B \boldsymbol D^{-1} \boldsymbol C}|\boldsymbol D| \cdot |k\boldsymbol E + \boldsymbol P|=0$，而 $|\boldsymbol D| \neq 0$，则 $|k\boldsymbol E + \boldsymbol P| = 0$。
	
	    但 $|k\boldsymbol E + \boldsymbol P|$ 是关于 $k$ 的 $n$ 次多项式（$n = 2^{2024}$），其首项系数为 $1$。该多项式若有无穷多个根，则必为零多项式，这与首项系数为 $1$ 矛盾。因此假设不成立，从而 $|\boldsymbol D| = 0$。

???+ info "2021-2022"
	设实三阶矩阵 $\boldsymbol A$ 满足 $6\boldsymbol A^3=11\boldsymbol A^2-6\boldsymbol A+\boldsymbol E$，矩阵 $\boldsymbol A$ 的特征值两两不同。
	
	（1）求 $\boldsymbol A$ 的特征值；
	
	（2）令 $\boldsymbol M_k=\boldsymbol A^k$，证明 $\lim\limits_{k\to +\infty}\boldsymbol M_k$ 存在；
	
	（3）证明 $\boldsymbol M=\lim\limits_{k\to +\infty}\boldsymbol M_k$ 是幂等矩阵，即 $\boldsymbol M^2=\boldsymbol M$。
	
	??? note "解答"
		- （1）
		
			$6\lambda^3-11\lambda^2+6\lambda-1=0$
			
			$(\lambda-1)(2\lambda-1)(3\lambda-1)=0$
			
			$\lambda_1=1$，$\lambda_2=\dfrac 1 2$，$\lambda_3=\dfrac 1 2$
		
		- （2）
		
			由（1）知 $\boldsymbol A$ 可相似对角化，则存在可逆矩阵 $\boldsymbol P$，使得 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda=\text{diag}( 1,\dfrac 1 2,\dfrac 1 3)$。
			
			$\boldsymbol P^{-1}\boldsymbol A^k\boldsymbol P=\boldsymbol \Lambda^k=\text{diag}(1,\dfrac 1 {2^k},\dfrac 1 {3^k})$
			
			$\boldsymbol A^k=\boldsymbol P\text{diag}(1,\dfrac 1 {2^k},\dfrac 1 {3^k})\boldsymbol P^{-1}$，易知 $\lim\limits_{k\to +\infty}\boldsymbol A^k$ 存在。
		
		- （3）
		
			$\boldsymbol M=\boldsymbol P\text{diag}(1,0,0)\boldsymbol P^{-1}$
			
			则 $\boldsymbol M^2=\boldsymbol P\text{diag}(1,0,0)\boldsymbol P^{-1}=\boldsymbol M$
	
	对于 $\boldsymbol A\in\mathbb P^{m\times n}$，$r\leq \min(m,n)$，证明：$r(\boldsymbol A)=r\Leftrightarrow$ 存在线性无关的列向量 $\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r$ 和线性无关的列向量 $\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_r$ 使得 $\boldsymbol A=\sum_{i=1}^r \boldsymbol \alpha_i\boldsymbol \beta_i^T$。
	
	??? note "解答"
		- $\Rightarrow$：
		
			由于 $r(\boldsymbol A)=r$，则存在 $m$ 阶可逆矩阵 $\boldsymbol P$ 和 $n$ 阶可逆矩阵 $\boldsymbol Q$，使得 $\boldsymbol A=\boldsymbol P^{-1}\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\\\boldsymbol O&\boldsymbol O\end{pmatrix}\boldsymbol Q^{-1}=\boldsymbol P^{-1}\begin{pmatrix}\boldsymbol E_r\\\boldsymbol O\end{pmatrix}\begin{pmatrix}\boldsymbol E_r&\boldsymbol O\end{pmatrix}\boldsymbol Q^{-1}$
			
			令 $\boldsymbol M_{m\times r}=\boldsymbol P^{-1}\begin{pmatrix}\boldsymbol E_r\\\boldsymbol O\end{pmatrix}=(\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r)$，$\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r$ 线性无关
			
			$\boldsymbol N_{r\times n}=(\boldsymbol E_r,\boldsymbol O)\boldsymbol Q^{-1}=\begin{pmatrix}\boldsymbol \beta_1^T\\\boldsymbol \beta_2^T\\\vdots\\\boldsymbol \beta_r^T\end{pmatrix}$，则 $\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_r$ 线性无关
			
			$\boldsymbol A=(\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r)\begin{pmatrix}\boldsymbol \beta_1^T\\\boldsymbol \beta_2^T\\\vdots\\\boldsymbol \beta_r^T\end{pmatrix}=\sum_{i=1}^r\boldsymbol \alpha_i\boldsymbol \beta_i^T$
		
		- $\Leftarrow$：
		
			$\boldsymbol A=(\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r)\begin{pmatrix}\boldsymbol \beta_1^T\\\boldsymbol \beta_2^T\\\vdots\\\boldsymbol \beta_r^T\end{pmatrix}=\sum_{i=1}^r\boldsymbol \alpha_i\boldsymbol \beta_i^T=\boldsymbol M_{m\times r}\boldsymbol N_{r\times n}$，由于 $\boldsymbol \alpha_1,\boldsymbol \alpha_2,\cdots,\boldsymbol \alpha_r$、$\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_r$ 线性无关，则 $r(\boldsymbol M)=r(\boldsymbol N)=r$
			
			则 $r(\boldsymbol A)\leq \min(r(\boldsymbol M),r(\boldsymbol N))=r$，$r(\boldsymbol A)\geq r(\boldsymbol M)+r(\boldsymbol N)-r=r$，故 $r(\boldsymbol A)=r$
	
	$\boldsymbol A$ 是 $n$ 阶实对称阵，$\boldsymbol B$ 是 $n$ 阶正定矩阵，证明：矩阵 $\boldsymbol A\boldsymbol B$ 的特征值均为实数。
	
	??? note "解答"
		存在可逆实矩阵 $\boldsymbol C$ 使得 $\boldsymbol B=\boldsymbol C^T\boldsymbol C$
		
		$\boldsymbol A\boldsymbol B=\boldsymbol A\boldsymbol C^T\boldsymbol C$
		
		$\boldsymbol C\boldsymbol A\boldsymbol B\boldsymbol C^{-1}=\boldsymbol C\boldsymbol A\boldsymbol C^T\boldsymbol C\boldsymbol C^{-1}=\boldsymbol C\boldsymbol A\boldsymbol C^T$，则 $\boldsymbol A\boldsymbol B\sim \boldsymbol C\boldsymbol A\boldsymbol C^T$，$\boldsymbol A\boldsymbol B$ 与 $\boldsymbol C\boldsymbol A\boldsymbol C^T$ 特征值相同
		
		又 $(\boldsymbol C\boldsymbol A\boldsymbol C^T)^T=\boldsymbol C\boldsymbol A\boldsymbol C^T$，故 $\boldsymbol C\boldsymbol A\boldsymbol C^T$ 是实对称矩阵，特征值均为实数
		
		因此 $\boldsymbol A\boldsymbol B$ 的特征值均为实数
	
		???+ tip
			将非对称的乘积 $\boldsymbol A\boldsymbol B$ 转化为实对称矩阵的相似矩阵
	
	$\boldsymbol A,\boldsymbol B$ 均为实矩阵，$\boldsymbol A^{-1}+\boldsymbol B^{-1}=(\boldsymbol A+\boldsymbol B)^{-1}$，证明：$(\boldsymbol A\boldsymbol B^{-1})^3=\boldsymbol E$。
	
	??? note "解答"
		$(\boldsymbol A+\boldsymbol B)(\boldsymbol A^{-1}+\boldsymbol B^{-1})=\boldsymbol E$
		
		得 $\boldsymbol A\boldsymbol B^{-1}+\boldsymbol B\boldsymbol A^{-1}=-\boldsymbol E$
		
		$\boldsymbol A\boldsymbol B^{-1}+(\boldsymbol A\boldsymbol B^{-1})^{-1}=-\boldsymbol E$
		
		记 $\boldsymbol X=\boldsymbol A\boldsymbol B^{-1}$，则 $\boldsymbol X=-\boldsymbol E-\boldsymbol X^{-1}$，下证 $\boldsymbol X^3=\boldsymbol E$
		
		$\boldsymbol X^2=-\boldsymbol X-\boldsymbol E$
		
		$\boldsymbol X^3=-\boldsymbol X^2-\boldsymbol X=-(-\boldsymbol X-\boldsymbol E)-\boldsymbol X=\boldsymbol E$

???+ info "2020-2021"
	设 $\boldsymbol A\in\mathbb R^{m\times n}$，$\boldsymbol B\in\mathbb R^{p\times n}$，$W=\{\boldsymbol A\boldsymbol X\in\mathbb R^m\mid \boldsymbol B\boldsymbol X=\boldsymbol 0_p\}$，其中 $\boldsymbol 0_p$ 是一个 $p$ 维零列向量。
	
	若 $r\begin{pmatrix}\boldsymbol A\\\boldsymbol B\end{pmatrix}=r_1$，$r(\boldsymbol B)=r_2$，求 $W$ 的维数。
	
	??? note "解答"
		
		设 $V=\{\boldsymbol X\mid \boldsymbol B\boldsymbol X=\boldsymbol 0\}$
	    
	    $\varphi:V\to W$，$\varphi(\boldsymbol X)=\boldsymbol A\boldsymbol X$
	    
	    $\text{Im}(\varphi)=\{\varphi(\boldsymbol X)\mid \boldsymbol X\in V\}=W$
	    
	    $\text{Ker}(\varphi)=\{\varphi(\boldsymbol X)=\boldsymbol 0\mid \boldsymbol X\in V\}$，则 $\dim \text{Ker}(\varphi)=n-r_1$
	    
	    又 $\dim \text{Im}(\varphi)+\dim\text{Ker}(\varphi)=\dim V$，故 $\dim W+(n-r_1)=(n-r_2)$，得 $\dim W=r_1-r_2$
		
	设 $\boldsymbol A$ 是一个 $n$ 阶正交矩阵，$\boldsymbol A+\boldsymbol E$ 可逆。证明：$\boldsymbol A(\boldsymbol E+\boldsymbol X)=\boldsymbol E-\boldsymbol X$ 的解 $\boldsymbol X$ 为实反对称矩阵。
	
	??? note "解答"
		$\boldsymbol X=(\boldsymbol A+\boldsymbol E)^{-1}(\boldsymbol E-\boldsymbol A)$
		
		$\boldsymbol X^T=(\boldsymbol E-\boldsymbol A^T)(\boldsymbol A^T+\boldsymbol E)^{-1}=(\boldsymbol E-\boldsymbol A^{-1})(\boldsymbol A^{-1}+\boldsymbol E)^{-1}=\boldsymbol A^{-1}(\boldsymbol A-\boldsymbol E)[\boldsymbol A^{-1}(\boldsymbol E+\boldsymbol A)]^{-1}=\boldsymbol A^{-1}(\boldsymbol A-\boldsymbol E)(\boldsymbol E+\boldsymbol A)^{-1}\boldsymbol A=(\boldsymbol A-\boldsymbol E)\boldsymbol A^{-1}\boldsymbol A(\boldsymbol E+\boldsymbol A)^{-1}=-(\boldsymbol A+\boldsymbol E)^{-1}(\boldsymbol E-\boldsymbol A)$
		
		故 $\boldsymbol X^T=-\boldsymbol X$

???+ info "2019-2020 春夏"
	设 $\boldsymbol A\in\mathbb R^{n\times n}$，证明：$\boldsymbol A$ 是反对称矩阵 $\Leftrightarrow \boldsymbol A \boldsymbol A^T=-\boldsymbol A^2$。
	
	??? note "解答"
		- $\Rightarrow$：代入即证
		- $\Leftarrow$：
			
			$\boldsymbol A\boldsymbol A^T=-\boldsymbol A^2\Rightarrow \boldsymbol A(\boldsymbol A^T+\boldsymbol A)=\boldsymbol O$
			
			$\boldsymbol A^T\boldsymbol A=-(\boldsymbol A^T)^2\Rightarrow \boldsymbol A^T(\boldsymbol A+\boldsymbol A^T)=\boldsymbol O$
			
			证明 $\boldsymbol A+\boldsymbol A^T=\boldsymbol O$，等价于证明 $\text{tr}((\boldsymbol A+\boldsymbol A^T)(\boldsymbol A+\boldsymbol A^T)^T)=0$
			
			$\text{tr}((\boldsymbol A+\boldsymbol A^T)(\boldsymbol A+\boldsymbol A^T)^T)=\text{tr}((\boldsymbol A+\boldsymbol A^T)(\boldsymbol A^T+\boldsymbol A))=\text{tr}(\boldsymbol A(\boldsymbol A^T+\boldsymbol A))+\text{tr}(\boldsymbol A^T(\boldsymbol A^T+\boldsymbol A))=0$，得证
			
			???+ tip
				证明 $\boldsymbol A=\boldsymbol O$，可以证明 $\text{tr}(\boldsymbol A\boldsymbol A^T)=0$
			
	设 $\boldsymbol C$ 是 $n$ 阶正定矩阵，$\boldsymbol B$ 是满足 $\boldsymbol B\boldsymbol B^T=-\boldsymbol B^2$ 的 $n$ 阶实矩阵，证明：$\boldsymbol B+\boldsymbol C$ 可逆。
	
	??? note "解答"
		由上一题知 $\boldsymbol B$ 是反对称矩阵，则 $\forall \boldsymbol x$ 有 $\boldsymbol x^T\boldsymbol B\boldsymbol x=0$
		
		$\boldsymbol C$ 是正定矩阵，则 $\forall \boldsymbol x\neq \boldsymbol 0$，$\boldsymbol x^T\boldsymbol C\boldsymbol x>0$
		
		故 $\boldsymbol x^T(\boldsymbol B+\boldsymbol C)\boldsymbol x>0$，则 $\boldsymbol B+\boldsymbol C$ 是正定矩阵，所有特征值大于 $0$，从而 $|\boldsymbol B+\boldsymbol C|\neq 0$，$\boldsymbol B+\boldsymbol C$ 可逆

???+ info "2019-2020 秋冬"
	设 $n$ 阶方阵 $\boldsymbol A=(a_{ij})_{n\times n}$，$a_{ij}=\sin(i+j)$（$i,j=1,2,\cdots,n$），求 $|\boldsymbol A|$。
	
	??? note "解答"
		$\boldsymbol A$ 的第 $j$ 列 $\begin{pmatrix}\sin(1+j)\\\sin(2+j)\\\vdots\\sin(n+j)\end{pmatrix}=\begin{pmatrix}\sin 1\\\sin 2\\\vdots\\\sin n\end{pmatrix}\cos j+\begin{pmatrix}\cos 1\\\cos 2\\\vdots\\\cos n\end{pmatrix}\sin j$
		
		则 $\boldsymbol A$ 的所有列向量均可由向量 $(\sin 1,\cdots,\sin n)^T$ 和 $(\cos 1,\cdots,\cos n)^T$ 线性表示
		
		- 当 $n\geq 3$ 时，$r(\boldsymbol A)\leq 2<n$，$|\boldsymbol A|=0$
		- 当 $n=1$ 时，$|\boldsymbol A|=a_{11}=\sin 2$
		- 当 $n=2$ 时，$|\boldsymbol A|=\begin{vmatrix}\sin 2&\sin 3\\\sin 3&\sin 4\end{vmatrix}=\sin 2\sin 4-\sin^2 3$
	
	设 $\mathbb R_+$ 为所有正实数组成的几何，加法和数乘定义如下：$\forall a,b\in \mathbb R_+$，$k\in\mathbb R$，$a\oplus b=ab$，$k\odot a=a^k$，已知 $\mathbb R_+$ 关于加法 $\oplus$ 和数乘 $\odot$ 构成一个实线性空间，求 $\mathbb R_+$ 的一组基和维数。
	
	??? note "解答"
		任取 $a\in\mathbb R_+,a\neq 1$，则 $\forall x\in\mathbb R_+$，令 $k=\log_a x\in\mathbb R$，则 $x=a^k=k\odot a$。故 $\mathbb R_+$ 中的任意向量均可由向量 $a$ 线性表示，故 $\dim \mathbb R_+=1$，且 $\mathbb R_+$ 中任意不等于 $1$ 的正实数均可作为一组基。

???+ info "2017-2018"
	设 $\boldsymbol A$ 为可逆的 $n$ 阶实对称矩阵，$\boldsymbol B$ 为 $n$ 阶反对称矩阵，$\boldsymbol A\boldsymbol B=\boldsymbol B\boldsymbol A$，证明：$\boldsymbol A-\boldsymbol B$ 可逆。

???+ info "2016-2017"
	设 $\boldsymbol A，\boldsymbol B$ 都是 $n$ 阶实方阵，证明：（1）$\boldsymbol A\boldsymbol B+\boldsymbol B$ 和 $\boldsymbol B\boldsymbol A+\boldsymbol B$ 有相同的特征值；（2）若 $\boldsymbol A\boldsymbol B=(\boldsymbol B-\boldsymbol A^T)\boldsymbol A$，则 $\boldsymbol A=\boldsymbol O$。
	
	证明：任何一个方阵可表示为一个幂等矩阵（满足 $\boldsymbol A^2=\boldsymbol A$ 的方阵）和一个可逆矩阵的乘积。

???+ info "2016-2017"
	已知 $\sum_{k=1}^n a_k=0$，求 $\boldsymbol A=\begin{pmatrix}a_1^2+1&a_1a_2+1&\cdots&a_1a_n+1\\a_2a_1+1&a_2^2+1&\cdots&a_2a_n+1\\\vdots&\vdots&\ddots&\vdots\\a_na_1+1&a_na_2+1&\cdots&a_n^2+1\end{pmatrix}$ 的特征值。