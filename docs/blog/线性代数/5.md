## 知识点

### 特征值与特征向量

- $\boldsymbol A\boldsymbol \xi=\lambda\boldsymbol \xi$，$\boldsymbol \xi\neq \boldsymbol 0$

    $\Leftrightarrow (\lambda\boldsymbol E-\boldsymbol A)\boldsymbol \xi=\boldsymbol 0$，$\boldsymbol \xi\neq \boldsymbol 0$

    $\Leftrightarrow(\lambda\boldsymbol E-\boldsymbol A)\boldsymbol X=\boldsymbol 0$ 有非零解

    $|\lambda\boldsymbol E-\boldsymbol A|=0$ 求出 $\lambda_i$

    将 $\lambda_i$ 代回，$(\lambda_i\boldsymbol E-\boldsymbol A)\boldsymbol X=\boldsymbol 0$ 求出所有 $\boldsymbol \xi\neq \boldsymbol 0$

- $\sum_{i=1}^n \lambda_i=\sum_{i=1}^n a_{ii}=\text{tr}(\boldsymbol A)$，$\prod_{i=1}^n\lambda_i=|\boldsymbol A|$

	$\boldsymbol A$ 可逆 $\Leftrightarrow$ $\prod_{i=1}^n\lambda_i\neq 0$

- $\boldsymbol A$ 有关矩阵的特征值和特征向量

    | 矩阵           | $\boldsymbol A$  | $k\boldsymbol A$ | $\boldsymbol A^k$ | $f(\boldsymbol A)$ | $\boldsymbol A^{-1}$ | $\boldsymbol A^*$                        | $\boldsymbol A^T$       | $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P$ | $\boldsymbol P^{-1}f(\boldsymbol A)\boldsymbol P$ |
    | -------------- | ---------------- | ---------------- | ----------------- | ------------------ | -------------------- | ---------------------------------------- | ----------------------- | ---------------------------------------------- | ------------------------------------------------- |
    | 特征值         | $\lambda$        | $k\lambda$       | $\lambda^k$       | $f(\lambda)$       | $\dfrac 1 \lambda$   | $\dfrac{\vert\boldsymbol A\vert}\lambda$ | $\lambda$               | $\lambda$                                      | $f(\lambda)$                                      |
    | 对应的特征向量 | $\boldsymbol\xi$ | $\boldsymbol\xi$ | $\boldsymbol\xi$  | $\boldsymbol\xi$   | $\boldsymbol\xi$     | $\boldsymbol\xi$                         | 不再是 $\boldsymbol\xi$ | $\boldsymbol P^{-1}\boldsymbol\xi$             | $\boldsymbol P^{-1}\boldsymbol\xi$                |

	表中若 $\lambda$ 在分母上，设 $\lambda\neq 0$。
	
	由于 $\boldsymbol O$ 的特征值是 $0$，故 $f(\boldsymbol A)=\boldsymbol O\Rightarrow f(\lambda)=0$。
	
	- 注：
	
        $\boldsymbol A^T$ 和 $\boldsymbol A$ 属于不同特征值的特征向量正交。
        
        若 $\boldsymbol A\boldsymbol B=\boldsymbol B\boldsymbol A$ 且 $\boldsymbol A$ 的特征值都是单根，则 $\boldsymbol A$ 的特征向量都是 $\boldsymbol B$ 的特征向量。

- $k$ 重特征值 $\lambda$ 至多只有 $k$ 个线性无关的特征向量。

- 若 $\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \lambda_s$ 属于的特征值两两不同，则 $\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \lambda_s$ 线性无关。

### 矩阵的相似（$\boldsymbol A\sim \boldsymbol B$）

- $\boldsymbol A\sim \boldsymbol B$：$\exists$ 可逆矩阵 $\boldsymbol P$，$\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol B$

	自反性，对称性，传递性
	
	若 $\boldsymbol A$ 可逆，则 $\boldsymbol A\boldsymbol B\sim\boldsymbol B\boldsymbol A$

- 性质：若 $\boldsymbol A\sim \boldsymbol B$，则

    | 矩阵                  | $\boldsymbol A\sim \boldsymbol B$ | $\boldsymbol A^k\sim \boldsymbol B^k$ | $f(\boldsymbol A)\sim f(\boldsymbol B)$ | $\boldsymbol A^{-1}\sim \boldsymbol B^{-1}$ | $\boldsymbol A^*\sim \boldsymbol B^*$ | $\boldsymbol A^T\sim \boldsymbol B^T$ |
    | --------------------- | --------------------------------- | ------------------------------------- | --------------------------------------- | ------------------------------------------- | ------------------------------------- | ------------------------------------- |
    | 是否相似              | √                                 | √                                     | √                                       | √（若 $\boldsymbol A$ 可逆）                | √（若 $\boldsymbol A$ 可逆）          | √                                     |
    | 对应 $\boldsymbol  P$ | $\boldsymbol  P$                  | $\boldsymbol  P$                      | $\boldsymbol P$                         | $\boldsymbol  P$                            | $\boldsymbol  P$                      | 不再是 $\boldsymbol P$                |

	$\boldsymbol A\sim \boldsymbol B,\boldsymbol B\sim \boldsymbol \Lambda\Rightarrow\boldsymbol A\sim\boldsymbol \Lambda$。
	
	$\boldsymbol A\sim\boldsymbol \Lambda,\boldsymbol B\sim\boldsymbol \Lambda \Rightarrow \boldsymbol A\sim\boldsymbol B$
	
	$\boldsymbol A\sim\boldsymbol C,\boldsymbol B\sim\boldsymbol D\Rightarrow\begin{pmatrix}\boldsymbol A&\boldsymbol O\\\boldsymbol O&\boldsymbol B\end{pmatrix}\sim\begin{pmatrix}\boldsymbol C&\boldsymbol O\\\boldsymbol O&\boldsymbol D\end{pmatrix}$

- 必要条件：

	$\boldsymbol A\sim \boldsymbol B\Rightarrow r(\boldsymbol A)=r(\boldsymbol B)$
	
	$\boldsymbol A\sim \boldsymbol B\Rightarrow |\boldsymbol A|=|\boldsymbol B|$
	
	$\boldsymbol A\sim \boldsymbol B\Rightarrow \text{tr}(\boldsymbol A)=\text{tr}(\boldsymbol B)$
	
	$\boldsymbol A\sim \boldsymbol B\Rightarrow |\lambda\boldsymbol E-\boldsymbol A|=|\lambda\boldsymbol E-\boldsymbol B|$（$\lambda_{\boldsymbol A}=\lambda_{\boldsymbol B}$）
	
	$\boldsymbol A\sim \boldsymbol B\Rightarrow \lambda\boldsymbol E-\boldsymbol A\sim\lambda\boldsymbol E-\boldsymbol B\Rightarrow n-r(\lambda\boldsymbol E-\boldsymbol A)=n-r(\lambda\boldsymbol E-\boldsymbol B)$

### 矩阵的相似对角化（$\boldsymbol A\sim\boldsymbol \Lambda$）

- 若 $\boldsymbol A\sim\boldsymbol \Lambda$，则 $\boldsymbol A$ 可相似对角化，$\boldsymbol \Lambda$ 为 $\boldsymbol A$ 的相似标准形。
- 设 $\boldsymbol P=(\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \xi_n)$，$\boldsymbol \Lambda=\text{diag}(\lambda_1,\lambda_2,\cdots,\lambda_n)$

	则 $\boldsymbol A\sim \boldsymbol \Lambda\Leftrightarrow \boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda\Leftrightarrow \boldsymbol A\boldsymbol P=\boldsymbol P\boldsymbol \Lambda$
	
	$\Leftrightarrow (\boldsymbol A\boldsymbol \xi_1,\boldsymbol A\boldsymbol \xi_2,\cdots,\boldsymbol A\boldsymbol \xi_n)=(\lambda_1\boldsymbol \xi_1,\lambda_2\boldsymbol \xi_2,\cdots,\lambda_n\boldsymbol \xi_n)$
	
	$\Leftrightarrow \boldsymbol A\boldsymbol \xi_i=\lambda_i\boldsymbol \xi_i$，$\boldsymbol \xi_i\neq \boldsymbol 0$（$i=1,2,\cdots,n$）
	
	故 $\boldsymbol A\sim\boldsymbol \Lambda\Leftrightarrow$ $\boldsymbol A$ 有 $n$ 个线性无关的特征向量。
	
	给出 $\boldsymbol A$ 求 $\boldsymbol P$ 和 $\boldsymbol \Lambda$：① 求 $\lambda,\boldsymbol \xi$，② 若有 $n$ 个无关的 $\boldsymbol \xi$，令 $\boldsymbol P=(\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \xi_n)$，则 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda=\text{diag}(\lambda_1,\lambda_2,\cdots,\lambda_n)$。

- 由 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda$，得 $\boldsymbol A=\boldsymbol P\boldsymbol \Lambda\boldsymbol P^{-1}$（给出特征值、特征向量，可求 $\boldsymbol A$），$\boldsymbol A^k=\boldsymbol P\boldsymbol \Lambda^k\boldsymbol P$，$f(\boldsymbol A)=\boldsymbol Pf(\boldsymbol \Lambda)\boldsymbol P^{-1}$

- 性质：若 $\boldsymbol A\sim \boldsymbol \Lambda$，则

    | 矩阵                  | $\boldsymbol A\sim \boldsymbol \Lambda$ | $\boldsymbol A^k\sim \boldsymbol \Lambda^k$ | $f(\boldsymbol A)\sim f(\boldsymbol \Lambda)$ | $\boldsymbol A^{-1}\sim \boldsymbol \Lambda^{-1}$ | $\boldsymbol A^*\sim \boldsymbol \Lambda^*$ | $\boldsymbol A^T\sim \boldsymbol \Lambda^T$ |
    | --------------------- | --------------------------------- | ------------------------------------- | --------------------------------------- | ------------------------------------------- | ------------------------------------- | ------------------------------------- |
    | 是否相似              | √                                 | √                                     | √                                       | √（若 $\boldsymbol A$ 可逆）                | √（若 $\boldsymbol A$ 可逆）          | √                                     |
    | 对应 $\boldsymbol  P$ | $\boldsymbol  P$                  | $\boldsymbol  P$                      | $\boldsymbol P$                         | $\boldsymbol  P$                            | $\boldsymbol  P$                      | 不再是 $\boldsymbol P$                |



- 充要条件：
	
	$\boldsymbol A\sim\boldsymbol \Lambda$
	
	$\Leftrightarrow\boldsymbol A$ 有 $n$ 个线性无关的特征向量
	
	$\Leftrightarrow\boldsymbol A$ 对应于每个 $n_i$ 重特征值都有 $n_i$ 个线性无关的特征向量
	
	$\Leftrightarrow$ $n_i=n-r(\lambda_i\boldsymbol E-\boldsymbol A)$
	
- 充分条件：

	$\boldsymbol A$ 是实对称矩阵 $\Rightarrow$ $\boldsymbol A\sim\boldsymbol \Lambda$

	$n$ 阶矩阵 $\boldsymbol A$ 有 $n$ 个不同特征值 $\Rightarrow$ $\boldsymbol A\sim\boldsymbol \Lambda$
	
	$\boldsymbol A^2-(k_1+k_2)\boldsymbol A+k_1k_2\boldsymbol E=\boldsymbol O$ 且 $k_1\neq k_2$（例如 $\boldsymbol A^2=k\boldsymbol A$，$\boldsymbol A^2=\boldsymbol E$）$\Rightarrow$ $\boldsymbol A\sim\boldsymbol\Lambda$
	
	$r(\boldsymbol A)=1$ 且 $\text{tr}(\boldsymbol A)\neq 0$ $\Rightarrow$ $\boldsymbol A\sim\boldsymbol \Lambda$

- 必要条件：

	$\boldsymbol A\sim\boldsymbol \Lambda\Rightarrow r(\boldsymbol A)=$ 非零特征值个数（重根按重数算）【$r(\boldsymbol A)=r(\boldsymbol P^{-1}\boldsymbol A\boldsymbol P)=r(\boldsymbol \Lambda)$】
	
- 否定条件：

	$\boldsymbol A\neq O$ 但 $\boldsymbol A^k=\boldsymbol O\,(k>1)$ $\Rightarrow$ $\boldsymbol A$ 不可相似对角化
	
	$\boldsymbol A$ 的特征值全为 $k$ 但 $\boldsymbol A\neq k\boldsymbol E$ $\Rightarrow$ $\boldsymbol A$ 不可相似对角化

### 实对称矩阵的对角化

- 实对称矩阵：$\boldsymbol A=\boldsymbol A^T$ 且组成 $\boldsymbol A$ 的元素均为实数
- 性质：若 $\boldsymbol A$ 是实对称矩阵，则

	- ① $\boldsymbol A$ 的特征值均为实数，特征向量均为实向量
	
	- ② 不同特征值对应的特征向量正交
	
        ??? note "证明"
            设 $\boldsymbol A\boldsymbol \xi_1=\lambda_1\boldsymbol \xi_1$，$\boldsymbol A\boldsymbol \xi_2=\lambda_2\boldsymbol \xi_2$，$\lambda_1\neq \lambda_2$，$\boldsymbol A^T=\boldsymbol A$
        
            $\lambda_1\boldsymbol \xi_2^T\boldsymbol \xi_1=\boldsymbol \xi_2^T\lambda_1\boldsymbol \xi_1=\boldsymbol \xi_2^T\boldsymbol A\boldsymbol \xi_1=\boldsymbol \xi_2^T\boldsymbol A^T\boldsymbol \xi_1=(\boldsymbol A\boldsymbol \xi_2)^T\boldsymbol \xi_1=(\lambda_2\boldsymbol \xi_2)^T\boldsymbol \xi_1=\lambda_2\boldsymbol \xi_2^T\boldsymbol \xi_1$
        
            即 $(\lambda_1-\lambda_2)\boldsymbol \xi_2^T\boldsymbol \xi_1=0$，又 $\lambda_1-\lambda_2\neq 0$，则 $\boldsymbol \xi_2^T\boldsymbol \xi_1=0$，$(\boldsymbol \xi_2,\boldsymbol \xi_1)=0$，$\boldsymbol \xi_2\perp \boldsymbol \xi_1$。
	
	- ③ $\boldsymbol A$ 相似于 $\boldsymbol \Lambda$：
	
		$\boldsymbol A$ 有 $n$ 个线性无关的特征向量
		
		$\boldsymbol A$ 对应于每个 $n_i$ 重特征值都有 $n_i$ 个线性无关的特征向量
		
		$n_i=n-r(\lambda_i\boldsymbol E-\boldsymbol A)$
		
		存在可逆矩阵 $\boldsymbol P=(\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \xi_n)$，使得 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda=\text{diag}(\lambda_1,\lambda_2,\cdots,\lambda_n)$
		
	- ④ $\boldsymbol A$ 正交相似于 $\boldsymbol \Lambda$：
		
		$\boldsymbol A$ 有 $n$ 个正交的单位特征向量
		
		$\boldsymbol A$ 对应于每个 $n_i$ 重特征值都有 $n_i$ 个正交的单位特征向量
		
		存在正交矩阵 $\boldsymbol Q$，使得 $\boldsymbol Q^{-1}\boldsymbol A\boldsymbol Q=\boldsymbol Q^T\boldsymbol A\boldsymbol Q=\boldsymbol \Lambda=\text{diag}(\lambda_1,\lambda_2,\cdots,\lambda_n)$
	
		求法：对每个 $\lambda_i$ 对应的基础解系分别正交化、单位化。
		
		注：首先，根据施密特正交化以及单位化的过程，正交化、单位化得到的特征向量仍然是 $\lambda_i$ 的特征向量，且两两正交；其次，实对称矩阵不同特征值的特征向量天然正交。但是对普通矩阵不能这样操作。
	
	- ⑤ $\boldsymbol A^*$，$\boldsymbol A^{-1}$，$\boldsymbol A^T$，$f(\boldsymbol A)$ 均为实对称矩阵

- $\boldsymbol A$ 为 $n$ 阶实对称矩阵 $\Leftrightarrow$ $\boldsymbol A$ 有 $n$ 个正交的特征向量

- 正交矩阵

	- ① $\boldsymbol A^T\boldsymbol A=\boldsymbol E\Leftrightarrow \boldsymbol A^{-1}=\boldsymbol A^T\Leftrightarrow$ $\boldsymbol A$ 由标准正交基组成 $\Leftrightarrow$ $\boldsymbol A^T$ 是正交矩阵 $\Leftrightarrow$
	
		$\boldsymbol A^{-1}$ 是正交矩阵 $\Leftrightarrow$ $\boldsymbol A^*$ 是正交矩阵 $\Leftrightarrow$ $-\boldsymbol A$ 是正交矩阵
	
	- ② 若 $\boldsymbol A,\boldsymbol B$ 为同阶正交矩阵，则 $\boldsymbol A\boldsymbol B$ 为正交矩阵，$\boldsymbol A+\boldsymbol B$ 不一定
	- ③ 若 $\boldsymbol A$ 为正交矩阵，则 $\boldsymbol A$ 的实特征值 $\in\{1,-1\}$

## 题目

- 若 $|a\boldsymbol A+b\boldsymbol E|=0$（或 $a\boldsymbol A+b\boldsymbol E$ 不可逆），$a\neq 0$，则 $|b\boldsymbol E-(-a\boldsymbol A)|=(-a)^n|-\dfrac b a \boldsymbol E-\boldsymbol A|=0$，则 $-\dfrac b a$ 是 $\boldsymbol A$ 的特征值。

	$|a\boldsymbol A^{m+1}+b\boldsymbol A^m|=0$ 且 $|\boldsymbol A|\neq 0$，则 $-\dfrac b a$ 是 $\boldsymbol A$ 的特征值。

- 已知 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol \Lambda$，则 $\boldsymbol P=(\boldsymbol \xi_1,\boldsymbol \xi_2,\cdots,\boldsymbol \xi_n)$，$\boldsymbol \Lambda=\text{diag}(\lambda_1,\lambda_2,\cdots,\lambda_n)$。 
- $\boldsymbol A\boldsymbol B=\boldsymbol O\Rightarrow \boldsymbol A(\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_n)=(\boldsymbol 0,\boldsymbol 0,\cdots,\boldsymbol 0)$，若 $\boldsymbol \beta_i\neq \boldsymbol 0$，则 $\boldsymbol \beta_i$ 为 $\boldsymbol A$ 的属于 $\lambda=0$ 的特征向量。

	$\boldsymbol A\boldsymbol B=k\boldsymbol B\Rightarrow \boldsymbol A(\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_n)=(k\boldsymbol \beta_1,k\boldsymbol \beta_2,\cdots,k\boldsymbol \beta_n)$，若 $\boldsymbol \beta_i\neq \boldsymbol 0$，则 $\boldsymbol \beta_i$ 为 $\boldsymbol A$ 的属于 $k$ 的特征向量。

	$\boldsymbol A\boldsymbol B=\boldsymbol C\Rightarrow \boldsymbol A(\boldsymbol \beta_1,\boldsymbol \beta_2,\cdots,\boldsymbol \beta_n)=(\boldsymbol \gamma_1,\boldsymbol \gamma_2,\cdots,\boldsymbol \gamma_n)\xlongequal{若}(\lambda_1\boldsymbol \beta_1,\lambda_2\boldsymbol \beta_2,\cdots,\lambda_n\boldsymbol \beta_n)$，若 $\boldsymbol \beta_i\neq \boldsymbol 0$，则 $\boldsymbol \beta_i$ 为 $\boldsymbol A$ 的属于 $\lambda_i$ 的特征向量。

- $\boldsymbol A\boldsymbol P=\boldsymbol P\boldsymbol B$ 且 $\boldsymbol P$ 可逆 $\Rightarrow$ $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol B$ $\Rightarrow$ $\boldsymbol A\sim \boldsymbol B$ $\Rightarrow$ $\lambda_{\boldsymbol A}=\lambda_{\boldsymbol B}$，$\boldsymbol \xi_{\boldsymbol A}=\boldsymbol P\boldsymbol \xi_{\boldsymbol B}$

	???+ example "例题"
		设 $\boldsymbol A\in\mathbb P^{3\times 3}$，$\boldsymbol \alpha\in \mathbb P^3$，$\boldsymbol \alpha,\boldsymbol A\boldsymbol \alpha,\boldsymbol A^2\boldsymbol \alpha$ 线性无关，且 $\boldsymbol A^3\boldsymbol \alpha=3\boldsymbol A\boldsymbol \alpha-2\boldsymbol A^2\boldsymbol \alpha$，求 $\boldsymbol A$ 的特征值。
		
		??? note "解答"
			令 $\boldsymbol P=(\boldsymbol \alpha,\boldsymbol A\boldsymbol \alpha,\boldsymbol A^2\boldsymbol \alpha)$，则 $\boldsymbol P$ 可逆
			
			$\boldsymbol A\boldsymbol P=\boldsymbol A(\boldsymbol \alpha,\boldsymbol A\boldsymbol \alpha,\boldsymbol A^2\boldsymbol \alpha)=(\boldsymbol A\boldsymbol \alpha,\boldsymbol A^2\boldsymbol \alpha,\boldsymbol A^3\boldsymbol \alpha)=(\boldsymbol A\boldsymbol \alpha,\boldsymbol A^2\boldsymbol \alpha,3\boldsymbol A\boldsymbol \alpha-2\boldsymbol A^2\boldsymbol \alpha)=(\boldsymbol \alpha,\boldsymbol A\alpha,\boldsymbol A^2\boldsymbol \alpha)\begin{pmatrix}0&0&0\\1&0&3\\0&1&-2\end{pmatrix}=\boldsymbol P\boldsymbol B$
			
			则 $\boldsymbol P^{-1}\boldsymbol A\boldsymbol P=\boldsymbol B$
			
			$|\lambda\boldsymbol E-\boldsymbol B|=0\Rightarrow \lambda=0,1,-3$
	
- $\boldsymbol A$ 的每行元素均为 $k$ $\Rightarrow$ $\boldsymbol A(1,1,\cdots,1)^T=k(1,1,\cdots,1)^T$ $\Rightarrow$ $k$ 是特征值，$(1,1,\cdots,1)^T$ 是 $\boldsymbol A$ 的属于 $k$ 的特征向量。
- 若 $\boldsymbol A=\boldsymbol\alpha\boldsymbol \beta^T$，则 $\lambda_1=\text{tr}(\boldsymbol A)=\boldsymbol A\beta^T\boldsymbol A\alpha$，$\lambda_2=\cdots=\lambda_n=0$。